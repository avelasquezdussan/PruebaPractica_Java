server:
  port: 8181

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pruebatecnica?zeroDateTimeBehavior = convertToNull&serverTimeZone
    username: root
    password: asd123..  
     